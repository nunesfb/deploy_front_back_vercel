{
  "version": 2,
  "builds": [
    {
      "src": "backend/**/*.ts",
      "use": "@vercel/node",
      "config": { "zeroConfig": true }
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "client/dist" }
    }
  ],
  "routes": [
    { "src": "/backend/(.*)", "dest": "/backend/server.ts" },
    { "src": "/(.*)", "dest": "/client/$1" }
  ]
}
